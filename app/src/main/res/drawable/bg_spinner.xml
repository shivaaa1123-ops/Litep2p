<?xml version="1.0" encoding="utf-8"?>
<!--
    Transport spinner background: a rounded well with a chevron affordance drawn by
    the layout (an ImageView overlay), so the plain framework Spinner still reads as
    a modern dropdown on the dark console surface.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/surface_2" />
    <corners android:radius="10dp" />
    <stroke
        android:width="1dp"
        android:color="@color/stroke" />
</shape>