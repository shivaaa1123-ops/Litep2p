<?xml version="1.0" encoding="utf-8"?>
<!-- Active voice call (LiteCall): status, duration, mute toggle, end button. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center_horizontal">

    <TextView
        android:id="@+id/callStatusText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:text="Connected" />

    <TextView
        android:id="@+id/callDurationText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:fontFamily="monospace"
        android:textSize="30sp"
        android:textColor="@color/text_secondary"
        android:text="00:00" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/callMuteButton"
            style="@style/Widget.LiteP2P.Button.Outlined"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingHorizontal="18dp"
            android:paddingVertical="8dp"
            android:text="Mute"
            app:icon="@drawable/ic_mic_off"
            app:iconPadding="6dp"
            app:iconTint="@color/text_secondary"
            android:textColor="@color/text_secondary" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/callEndButton"
            style="@style/Widget.LiteP2P.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:insetTop="0dp"
            android:insetBottom="0dp"
            android:paddingHorizontal="18dp"
            android:paddingVertical="8dp"
            android:text="End"
            android:textColor="@android:color/white"
            app:backgroundTint="@color/call_end_red"
            app:icon="@drawable/ic_phone_end"
            app:iconPadding="6dp"
            app:iconTint="@android:color/white" />

    </LinearLayout>

</LinearLayout>
