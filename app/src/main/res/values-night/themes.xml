<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        LiteP2P is intentionally dark-only (it mirrors a terminal running the desktop
        peer), so the night theme adds no overrides: the base Material 3 dark theme
        in values/themes.xml applies in both light and dark system modes.
    -->
</resources>